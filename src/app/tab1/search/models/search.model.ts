export interface IKeywords {
    searchText?: string;
    keyText?: string;
  }
export interface IitemSearch {
    imagen?: string,
    nombre?: string,
    texto?: string,
    mediaVal?: number,
    numVal?: number,
    precio: number,
}